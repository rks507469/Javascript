// updating the values
let counter = 0;

counter += 1; // same as counter = counter + 1
// counter++; => post increment
counter *= 1; // same as counter = counter * 1
counter /= 1; // same as counter = counter / 1
counter -= 1; // same as counter = counter - 1
// counter--; => post decrement